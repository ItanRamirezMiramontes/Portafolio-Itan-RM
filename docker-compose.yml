version: "3.8"

services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_SITE_URL: http://localhost:3000
    ports:
      - "3000:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
